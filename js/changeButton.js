// document.getElementById("add-fa").addEventListener(
//     "click",
//     function(event) {
//         if (event.target.value === "Add to Favourite Authors") {
//             event.target.value = "Remove from Favourite Authors";
//         } else {
//             event.target.value = "Add to Favourite Authors";
//         }
//     },
//     false
// );

document.getElementById("add-fa-0").addEventListener(
    "click",
    function(event) {
        if (event.target.value === "Add to Favourite Authors") {
            event.target.value = "Remove from Favourite Authors";
        } else {
            event.target.value = "Add to Favourite Authors";
        }
    },
    false
);
document.getElementById("add-fa-1").addEventListener(
    "click",
    function(event) {
        if (event.target.value === "Add to Favourite Authors") {
            event.target.value = "Remove from Favourite Authors";
        } else {
            event.target.value = "Add to Favourite Authors";
        }
    },
    false
);
document.getElementById("add-fa-2").addEventListener(
    "click",
    function(event) {
        if (event.target.value === "Add to Favourite Authors") {
            event.target.value = "Remove from Favourite Authors";
        } else {
            event.target.value = "Add to Favourite Authors";
        }
    },
    false
);
document.getElementById("add-fa-3").addEventListener(
    "click",
    function(event) {
        if (event.target.value === "Add to Favourite Authors") {
            event.target.value = "Remove from Favourite Authors";
        } else {
            event.target.value = "Add to Favourite Authors";
        }
    },
    false
);
document.getElementById("add-fa-4").addEventListener(
    "click",
    function(event) {
        if (event.target.value === "Add to Favourite Authors") {
            event.target.value = "Remove from Favourite Authors";
        } else {
            event.target.value = "Add to Favourite Authors";
        }
    },
    false
);